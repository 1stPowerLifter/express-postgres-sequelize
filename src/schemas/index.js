const userSchemas = require('./userSchema');
const postSchemas = require('./postSchema');
const categorySchemas = require('./categorySchema');
const roleSchemas = require('./roleSchema');
const tagSchemas = require('./tagSchema');

module.exports = {
  userSchemas,
  postSchemas,
  categorySchemas,
  roleSchemas,
  tagSchemas,
};
